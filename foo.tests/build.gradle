def resolveTask = tasks.named("resolve.foo.tests") {
    outputBndrun = layout.buildDirectory.file("foo.tests.bndrun")
}
tasks.named("testOSGi") {
    bndrun = resolveTask.flatMap { it.outputBndrun }
}